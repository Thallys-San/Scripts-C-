#include <stdio.h>
#include <iostream>

using namespace std;

int main()
{
    int i, num, x;
    cout << "Escolha uma opcao de idioma: ";
    for (i=1; i<=x2; i++) {
        num= i%2;
        if (num%2 ==0)
           cout << "Numeros pares ";
    cin >> i;
    }
    return 0;
}
