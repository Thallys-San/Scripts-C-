#include <stdio.h>
#include <iostream>

using namespace std;

int main()
{
    int i, num, x;
    cout << "Os numeros pares de 1 a 100 sao: ";
    for (i=1; i<=x2; i++) {
        num= i%2;
        if (num%2 ==0)
           cout << "Numeros pares ";
    cin >> i;
    }
    return 0;
}
